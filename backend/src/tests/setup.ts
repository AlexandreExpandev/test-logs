// This file is for global test setup, such as:
// - Mocking modules (e.g., database connections, external services)
// - Setting up a test database
// - Starting and stopping the server before and after tests

beforeAll(async () => {
  // Global setup before all tests run
});

afterAll(async () => {
  // Global teardown after all tests have run
});
