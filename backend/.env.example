# Application Configuration
NODE_ENV=development
PORT=3000

# Database Configuration (SQL Server)
DB_HOST=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=your_strong_password
DB_NAME=GuessNumberDB
DB_ENCRYPT=false

# Security
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRATION=1h
