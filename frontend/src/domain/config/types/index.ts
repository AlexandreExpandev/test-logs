/**
 * @file Defines types for the config domain.
 */

/**
 * @interface GameConfig
 * @summary Represents the configuration for the game range.
 */
export interface GameConfig {
  minRange: number;
  maxRange: number;
}
